/**
 * Returns the value of an object's property.
 *
 * Returns undefined if x is not an object.
 *
 * @template A
 * @param {string} key object property key
 * @returns {(x: A) => any}
 */
export function prop<A>(key: string): (x: A) => any;

/**
 * Applies an array of functions sequentially on a value.
 *
 * @template A, B
 * @param {Function[]} fs array of functions
 * @returns {(x: A) => B}
 */
export function pipe<A, B>(fs: Function[]): (x: A) => B;

/**
 * Deepmerge from https://github.com/TehShrike/deepmerge.
 *
 * @template A, B
 * @param {Partial<A>} x
 * @returns {(y: Partial<B>) => A & B}
 */
export function merge<A, B>(x: Partial<A>): (y: Partial<B>) => A & B;

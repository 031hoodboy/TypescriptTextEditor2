export type ThemeMapping = import('./types.js').ThemeMapping;
/**
 * @typedef {import('./types.js').ThemeMapping} ThemeMapping
 */
/**
 * A mapping of `ThemeProperty` to one or more `CssProperty`.
 *
 * @type {ThemeMapping}
 */
export const themeMapping: ThemeMapping;
